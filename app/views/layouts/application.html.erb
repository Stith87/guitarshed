<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <meta name="keywords" content="<% if defined?(cms_page_content(:keyworks)) %><%= cms_page_content(:meta_keywords) %><% else %><%= meta_keywords %>" /> 
    <% end %>

    <meta name="description" content="<% if defined?(cms_page_content(:meta_description)) %><%= cms_page_content(:meta_description) %><% else %><%= meta_description %>
    <% end %>">
    <meta name="description" content="" />
    <%= stylesheet_link_tag "application", media: "all" %>
    <link href='http://fonts.googleapis.com/css?family=Roboto+Slab:700' rel='stylesheet' type='text/css'>
    <%= javascript_include_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-39940799-1', 'guitarshed.net');
      ga('send', 'pageview');

    </script>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <div class="container-full">

      <main role="main">
          
         <%= render 'layouts/messages' %>
         <%= yield %>
         <%= render 'layouts/footer' %>
      </main>
    </div>


  </body>
</html>
